- page_title "滥用报告"
%h3.page-title 滥用报告
%hr
- if @abuse_reports.present?
  .table-holder
    %table.table
      %thead
        %tr
          %th 用户
          %th 被谁报告
          %th 消息
          %th 主要行为
          %th
      = render @abuse_reports
  = paginate @abuse_reports
- else
  %h4 没有滥用报告
